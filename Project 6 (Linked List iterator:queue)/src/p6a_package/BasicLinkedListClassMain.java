package p6a_package;

import p6_package.BasicLinkedListClass;
import p6_package.IteratorClass;
import p6_package.QueueClass;

public class BasicLinkedListClassMain
{
	public static void main(String[] args) 
	{
		/**
				BasicLinkedListClass linkedList = new BasicLinkedListClass();
				
				linkedList.setAtIndex(0, 0, REPLACE);
				//linkedList.runDiagnosticDisplay( false );
				linkedList.runDiagnosticDisplay( true );
				
				linkedList.setAtIndex(0, 9, INSERT_AFTER);
				//linkedList.runDiagnosticDisplay( false );
				linkedList.runDiagnosticDisplay( true );
				
				linkedList.setAtIndex(0, 5, INSERT_AFTER);
				//linkedList.runDiagnosticDisplay( false );
				linkedList.runDiagnosticDisplay( true );
				
				linkedList.setAtIndex(0, 6, INSERT_AFTER);
				//linkedList.runDiagnosticDisplay( false );
				linkedList.runDiagnosticDisplay( true );
				
				linkedList.setAtIndex(3, 35, INSERT_AFTER);
				//linkedList.runDiagnosticDisplay( false );
				linkedList.runDiagnosticDisplay( true );
				
				linkedList.setAtIndex(1, 58, INSERT_BEFORE);
				//linkedList.runDiagnosticDisplay( false );
				linkedList.runDiagnosticDisplay( true );
				*/
		
	}
}